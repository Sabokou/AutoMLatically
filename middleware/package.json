{
  "name": "middleware",
  "version": "1.0.0",
  "description": "## to setup the middleware functionallity certain packages are required:",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx nodemon server.js"
  },
  "author": "",
  "license": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "file": "^0.2.2",
    "multer": "^1.4.5-lts.1",
    "node": "^17.7.2",
    "nodemon": "^2.0.16",
    "upload": "^1.1.1"
  }
}
